require 'rails_helper'

# This spec was generated by rspec-rails when you ran the scaffold generator.
# It demonstrates how one might use RSpec to test the controller code that
# was generated by Rails when you ran the scaffold generator.
#
# It assumes that the implementation code is generated by the rails scaffold
# generator. If you are using any extension libraries to generate different
# controller code, this generated spec may or may not pass.
#
# It only uses APIs available in rails and/or rspec-rails. There are a number
# of tools you can use to make these specs even more expressive, but we're
# sticking to rails and rspec-rails APIs to keep things simple and stable.

RSpec.describe "/repair_sections", type: :request do
  
  # This should return the minimal set of attributes required to create a valid
  # RepairSection. As you add validations to RepairSection, be sure to
  # adjust the attributes here as well.
  let(:valid_attributes) {
    skip("Add a hash of attributes valid for your model")
  }

  let(:invalid_attributes) {
    skip("Add a hash of attributes invalid for your model")
  }

  describe "GET /index" do
    it "renders a successful response" do
      RepairSection.create! valid_attributes
      get repair_sections_url
      expect(response).to be_successful
    end
  end

  describe "GET /show" do
    it "renders a successful response" do
      repair_section = RepairSection.create! valid_attributes
      get repair_section_url(repair_section)
      expect(response).to be_successful
    end
  end

  describe "GET /new" do
    it "renders a successful response" do
      get new_repair_section_url
      expect(response).to be_successful
    end
  end

  describe "GET /edit" do
    it "renders a successful response" do
      repair_section = RepairSection.create! valid_attributes
      get edit_repair_section_url(repair_section)
      expect(response).to be_successful
    end
  end

  describe "POST /create" do
    context "with valid parameters" do
      it "creates a new RepairSection" do
        expect {
          post repair_sections_url, params: { repair_section: valid_attributes }
        }.to change(RepairSection, :count).by(1)
      end

      it "redirects to the created repair_section" do
        post repair_sections_url, params: { repair_section: valid_attributes }
        expect(response).to redirect_to(repair_section_url(RepairSection.last))
      end
    end

    context "with invalid parameters" do
      it "does not create a new RepairSection" do
        expect {
          post repair_sections_url, params: { repair_section: invalid_attributes }
        }.to change(RepairSection, :count).by(0)
      end

      it "renders a response with 422 status (i.e. to display the 'new' template)" do
        post repair_sections_url, params: { repair_section: invalid_attributes }
        expect(response).to have_http_status(:unprocessable_content)
      end
    end
  end

  describe "PATCH /update" do
    context "with valid parameters" do
      let(:new_attributes) {
        skip("Add a hash of attributes valid for your model")
      }

      it "updates the requested repair_section" do
        repair_section = RepairSection.create! valid_attributes
        patch repair_section_url(repair_section), params: { repair_section: new_attributes }
        repair_section.reload
        skip("Add assertions for updated state")
      end

      it "redirects to the repair_section" do
        repair_section = RepairSection.create! valid_attributes
        patch repair_section_url(repair_section), params: { repair_section: new_attributes }
        repair_section.reload
        expect(response).to redirect_to(repair_section_url(repair_section))
      end
    end

    context "with invalid parameters" do
      it "renders a response with 422 status (i.e. to display the 'edit' template)" do
        repair_section = RepairSection.create! valid_attributes
        patch repair_section_url(repair_section), params: { repair_section: invalid_attributes }
        expect(response).to have_http_status(:unprocessable_content)
      end
    end
  end

  describe "DELETE /destroy" do
    it "destroys the requested repair_section" do
      repair_section = RepairSection.create! valid_attributes
      expect {
        delete repair_section_url(repair_section)
      }.to change(RepairSection, :count).by(-1)
    end

    it "redirects to the repair_sections list" do
      repair_section = RepairSection.create! valid_attributes
      delete repair_section_url(repair_section)
      expect(response).to redirect_to(repair_sections_url)
    end
  end
end
