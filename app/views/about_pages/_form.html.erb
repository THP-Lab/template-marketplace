<%= form_with(model: about_page) do |form| %>
  <% if local_assigns[:redirect_to].present? %>
    <%= hidden_field_tag :redirect_to, local_assigns[:redirect_to] %>
  <% end %>

  <% if about_page.errors.any? %>
    <div class="alert alert-danger">
      <h2 class="h6 mb-2"><%= pluralize(about_page.errors.count, "erreur") %> empêchent l’enregistrement :</h2>
      <ul class="mb-0">
        <% about_page.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="mb-3">
    <%= form.label :section_type, "Partie de la page À propos", class: "form-label" %>
    <%= form.select :section_type,
                    AboutPage.section_options_for_select,
                    {},
                    class: "form-select" %>
  </div>

  <div class="mb-3">
    <%= form.label :title, "Titre", class: "form-label" %>
    <%= form.text_field :title, class: "form-control" %>
  </div>

  <div class="mb-3">
    <%= form.label :content, "Contenu", class: "form-label" %>
    <%= form.text_area :content, class: "form-control", rows: 4 %>
  </div>

  <% if about_page.persisted? %>
    <div class="mb-3">
      <%= form.label :position, "Position", class: "form-label" %>
      <%= form.number_field :position, class: "form-control" %>
    </div>
  <% end %>

  <div class="mb-3">
    <%= form.label :image, "Image", class: "form-label" %>
    <%= form.file_field :image, class: "form-control" %>
    <% if about_page.image.attached? %>
      <div class="mt-2">
        <%= attachment_thumb(about_page.image, variant_options: { resize_to_limit: [200, 200] }, class: "img-thumbnail") %>
      </div>
      <div class="form-check mt-2">
        <%= check_box_tag "about_page[remove_image]", "1", false, class: "form-check-input", id: "about_page_remove_image" %>
        <%= label_tag "about_page_remove_image", "Supprimer l’image actuelle", class: "form-check-label" %>
      </div>
    <% end %>
  </div>

  <div class="d-flex gap-2">
    <%= form.submit "Enregistrer", class: "btn btn-primary" %>
  </div>
<% end %>
