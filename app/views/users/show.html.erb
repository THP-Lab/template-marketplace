<h1>Profil de <%= @user.email %></h1>

<p>current_user.id : <%= current_user&.id %></p>
<p>@user.id        : <%= @user.id %></p>
<p>current_user == @user ? <%= (current_user == @user).inspect %></p>

<h2>Commandes : </h2>
<ul>
  <% @orders.each do |order| %>
    <li>
      Commande nº <%= order.id %> –
      user_id de la commande : <%= order.user_id %> –
      Statut : <%= order.status %> –
      Montant total : <%= order.total_amount %> –
      Date : <%= order.order_date %>
    </li>
  <% end %>
</ul>