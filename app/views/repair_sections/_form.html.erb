<%= form_with(model: repair_section) do |form| %>
  <% if repair_section.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(repair_section.errors.count, "error") %> prohibited this repair_section from being saved:</h2>

      <ul>
        <% repair_section.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :title, style: "display: block" %>
    <%= form.text_field :title %>
  </div>

  <div>
    <%= form.label :content, style: "display: block" %>
    <%= form.textarea :content %>
  </div>

  <div>
    <%= form.label :position, style: "display: block" %>
    <%= form.number_field :position %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
