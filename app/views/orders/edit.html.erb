<% content_for :title, "Admin - Modifier une commande" %>

<%= admin_page(title: "Modifier une commande") do %>
  <%= render "form", order: @order %>

  <div class="mt-3 d-flex gap-2">
    <%= link_to "Voir la commande", @order, class: "btn btn-outline-secondary btn-sm" %>
    <%= link_to "Retour aux commandes", admin_orders_path, class: "btn btn-outline-secondary btn-sm" %>
  </div>
<% end %>
